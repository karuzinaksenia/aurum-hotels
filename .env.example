# MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/aurum_hotels

# API
PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# JWT (обязательно смените в production)
JWT_SECRET=your-super-secret-key-min-32-chars

# Frontend build (Vercel / same-origin — можно не задавать)
# VITE_API_URL=/api
